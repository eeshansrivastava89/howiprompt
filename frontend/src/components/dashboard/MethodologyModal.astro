---
// Methodology modal: snap-scroll brew guide with persona explorer
---
<div class="modal-overlay" id="methodologyModal" role="dialog" aria-modal="true" aria-labelledby="methodologyTitle">
  <div class="meth-modal">
    <div class="meth-progress"><div class="meth-progress-fill" id="methProgressFill"></div></div>

    <!-- Header -->
    <div class="meth-header">
      <div class="meth-header-left">
        <div class="meth-logo">☕</div>
        <div>
          <div class="meth-header-title" id="methodologyTitle">The Brew Guide</div>
          <div class="meth-header-sub">How your prompting style gets scored</div>
        </div>
      </div>
      <button class="modal-close" onclick="closeMethodology()" aria-label="Close methodology modal">
        <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
        </svg>
      </button>
    </div>

    <!-- Snap-scroll container -->
    <div class="meth-scroll" id="methScroll">

      <!-- S1: Intro + Pipeline -->
      <section class="meth-snap meth-s1" data-meth-section="0">
        <div class="meth-s1-inner">
          <div class="meth-hero">
            <div class="meth-hero-kicker meth-reveal">☕ Methodology</div>
            <h1 class="meth-reveal d1">From raw chats to your <em>signature blend</em></h1>
            <p class="meth-hero-sub meth-reveal d2">We harvest your local chat history, roast it through embeddings, and brew a persona that captures how you prompt.</p>
          </div>

          <div class="meth-pipeline-wrap meth-reveal d3">
            <div class="meth-rule">The Brewing Process</div>
            <div class="meth-process">
              <div class="meth-step">
                <div class="meth-step-icon">🫘</div>
                <h3>Harvest</h3>
                <p>Read local histories from Claude Code, Codex, Copilot Chat, Cursor, and LM Studio.</p>
              </div>
              <div class="meth-step">
                <div class="meth-step-icon">⚙️</div>
                <h3>Sort</h3>
                <p>Keep human-authored prompts. Remove system messages, tool output, and assistant text.</p>
              </div>
              <div class="meth-step">
                <div class="meth-step-icon">🔥</div>
                <h3>Roast</h3>
                <p>Embed each prompt with bge-small-en-v1.5 locally. Score 7 metrics via cosine similarity.</p>
              </div>
              <div class="meth-step">
                <div class="meth-step-icon">☕</div>
                <h3>Brew</h3>
                <p>Four axes — Precision, Curiosity, Tenacity, Trust — map to the persona that fits you.</p>
              </div>
            </div>
          </div>

          <div class="meth-scroll-hint meth-reveal d4">
            <span>Scroll</span>
            <div class="meth-scroll-arrow"></div>
          </div>
        </div>
      </section>

      <!-- S2: Tasting Notes -->
      <section class="meth-snap meth-s2" data-meth-section="1">
        <div class="meth-s2-inner">
          <div class="meth-s2-header meth-reveal">
            <h2>Tasting Notes</h2>
            <p>Seven metrics across four dimensions. Each one uses embeddings — not regex or keyword matching.</p>
          </div>

          <div class="meth-taste-grid">
            <div class="meth-taste-card meth-reveal d1" style="--tc:#5c3d2e">
              <div class="meth-taste-cat">Volume</div>
              <div class="meth-taste-list">
                <div class="meth-taste-item"><span class="meth-taste-name">Prompts</span><span class="meth-taste-note">Total messages</span></div>
                <div class="meth-taste-item"><span class="meth-taste-name">Conversations</span><span class="meth-taste-note">Distinct threads</span></div>
                <div class="meth-taste-item"><span class="meth-taste-name">Words</span><span class="meth-taste-note">Total typed</span></div>
              </div>
            </div>
            <div class="meth-taste-card meth-reveal d2" style="--tc:#7b5a42">
              <div class="meth-taste-cat">Timing</div>
              <div class="meth-taste-list">
                <div class="meth-taste-item"><span class="meth-taste-name">Peak hour</span><span class="meth-taste-note">Most active time</span></div>
                <div class="meth-taste-item"><span class="meth-taste-name">Night owl</span><span class="meth-taste-note">11pm–4am share</span></div>
                <div class="meth-taste-item"><span class="meth-taste-name">Trend</span><span class="meth-taste-note">Volume over time</span></div>
              </div>
            </div>
            <div class="meth-taste-card meth-reveal d3" style="--tc:#a08060">
              <div class="meth-taste-cat">Style</div>
              <div class="meth-taste-list">
                <div class="meth-taste-item"><span class="meth-taste-name">Human in Loop</span><span class="meth-taste-note">How you steer</span></div>
                <div class="meth-taste-item"><span class="meth-taste-name">Vibe Coder</span><span class="meth-taste-note">Structured vs. free</span></div>
                <div class="meth-taste-item"><span class="meth-taste-name">Politeness</span><span class="meth-taste-note">Tone warmth</span></div>
              </div>
            </div>
            <div class="meth-taste-card meth-reveal d4" style="--tc:#c4a882">
              <div class="meth-taste-cat">Persona Axes</div>
              <div class="meth-taste-list">
                <div class="meth-taste-item"><span class="meth-taste-name">Precision</span><span class="meth-taste-note">Instruction detail</span></div>
                <div class="meth-taste-item"><span class="meth-taste-name">Curiosity</span><span class="meth-taste-note">Exploration</span></div>
                <div class="meth-taste-item"><span class="meth-taste-name">Tenacity</span><span class="meth-taste-note">Depth</span></div>
                <div class="meth-taste-item"><span class="meth-taste-name">Trust</span><span class="meth-taste-note">Delegation</span></div>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- S3: Persona Explorer -->
      <section class="meth-snap meth-s3" data-meth-section="2">
        <div class="meth-s3-inner">
          <div class="meth-s3-header meth-reveal">
            <h2>House Blends — The 5 Personas</h2>
            <p>Your dominant axis determines which blend fits your prompting pattern.</p>
          </div>

          <div class="meth-persona-layout meth-reveal d1">
            <div class="meth-persona-list" id="methPersonaList"></div>
            <div class="meth-pd" id="methPersonaDetail"></div>
          </div>
        </div>
      </section>
    </div>

    <!-- Bottom bar -->
    <div class="meth-bottom">
      <span class="meth-bottom-note">All analysis stays local. Persona is a pattern summary, not a fixed identity.</span>
      <div class="meth-dot-nav" id="methDotNav">
        <button class="meth-dot active" data-meth-idx="0"></button>
        <button class="meth-dot" data-meth-idx="1"></button>
        <button class="meth-dot" data-meth-idx="2"></button>
      </div>
    </div>
  </div>
</div>

<script>
(function() {
  const IMG = '/images/';
  // Persona UX copy — intentionally more casual than pipeline definitions in
  // src/pipeline/models.ts. Names and keys must stay in sync; descriptions
  // are tailored for the modal context (signature prompts, flavor notes).
  const personas = [
    { key:'architect', img:IMG+'char_architect.png', name:'The Architect',
      badge:'Precision-led', trait:'Structured, exact, review-heavy',
      accent:'#7b5a42', accentDark:'#c4a07a',
      gradient:'linear-gradient(160deg,#e8ddd2,#ddd0c2)',
      gradientDark:'linear-gradient(160deg,#342c24,#2a2218)',
      desc:'You think in specs, constraints, and acceptance criteria. You want the AI to follow the plan closely and you review outputs carefully.',
      prompt:'\u201CUse the existing component API. Keep the interaction model unchanged. Add keyboard support, tests, and a migration note.\u201D',
      liner:'You like the model to be powerful, but not unsupervised.',
      axes:{Precision:91,Curiosity:48,Tenacity:67,Trust:31} },
    { key:'explorer', img:IMG+'char_explorer.png', name:'The Explorer',
      badge:'Curiosity-led', trait:'Investigative, branching, exploratory',
      accent:'#3f7a6e', accentDark:'#6abfae',
      gradient:'linear-gradient(160deg,#d4e8e3,#c2ddd6)',
      gradientDark:'linear-gradient(160deg,#222e2b,#1a2624)',
      desc:'You probe, branch, and investigate. Your prompts open doors instead of closing them, and you use the AI to discover rather than execute.',
      prompt:'\u201CCompare these three approaches, surface tradeoffs, and tell me what an experienced engineer would worry about.\u201D',
      liner:'You use the model as an idea space, not just a task runner.',
      axes:{Precision:63,Curiosity:92,Tenacity:59,Trust:46} },
    { key:'commander', img:IMG+'char_commander.png', name:'The Commander',
      badge:'Direct + decisive', trait:'Efficient, concise, action-first',
      accent:'#41608e', accentDark:'#7da4d4',
      gradient:'linear-gradient(160deg,#d4dde8,#c2cedd)',
      gradientDark:'linear-gradient(160deg,#222830,#1a2028)',
      desc:'You issue clear, efficient instructions and move fast. Less discussion, more execution. You like momentum and concise control.',
      prompt:'\u201CPatch this regression, keep the API stable, and show me the exact files changed.\u201D',
      liner:'You prefer decisive progress over collaborative wandering.',
      axes:{Precision:82,Curiosity:38,Tenacity:34,Trust:49} },
    { key:'partner', img:IMG+'char_partner.png', name:'The Partner',
      badge:'Tenacity-led', trait:'Iterative, collaborative, persistent',
      accent:'#8a5d73', accentDark:'#c48aa8',
      gradient:'linear-gradient(160deg,#e8d8e0,#ddc8d4)',
      gradientDark:'linear-gradient(160deg,#2e2428,#261c22)',
      desc:'You stay with problems. Your prompts build through iteration, refinement, and back-and-forth until the result feels right.',
      prompt:'\u201CBetter, but not there yet. Keep the tone, reduce complexity, and give me a second version with a stronger opening.\u201D',
      liner:'You keep pushing until the output finally clicks.',
      axes:{Precision:61,Curiosity:58,Tenacity:93,Trust:52} },
    { key:'delegator', img:IMG+'char_delegator.png', name:'The Delegator',
      badge:'Trust-led', trait:'Outcome-first, delegative, leveraged',
      accent:'#8a7a44', accentDark:'#c4b46a',
      gradient:'linear-gradient(160deg,#e8e2d0,#ddd6c0)',
      gradientDark:'linear-gradient(160deg,#2c2a1e,#24221a)',
      desc:'You define the goal, trust the system, and let the AI fill in the path. You optimize for outcomes more than hand-holding.',
      prompt:'\u201CBuild the first draft end-to-end, choose sensible defaults, and then tell me what assumptions you made.\u201D',
      liner:'You want lift and momentum, even if it means giving up some control.',
      axes:{Precision:44,Curiosity:46,Tenacity:57,Trust:94} }
  ];

  const isDark = () => document.documentElement.classList.contains('dark');
  let activeKey = 'architect';
  const listEl = document.getElementById('methPersonaList');
  const detailEl = document.getElementById('methPersonaDetail');
  if (!listEl || !detailEl) return;

  function renderList() {
    listEl.innerHTML = personas.map(p => `
      <button class="meth-p-thumb ${p.key===activeKey?'active':''}" data-pkey="${p.key}"
              style="--pa:${isDark()?p.accentDark:p.accent}">
        <div class="meth-p-thumb-img"><img src="${p.img}" alt="${p.name}"/></div>
        <div class="meth-p-thumb-text">
          <div class="meth-p-thumb-name">${p.name.replace('The ','')}</div>
          <div class="meth-p-thumb-trait">${p.trait}</div>
        </div>
      </button>`).join('');
  }

  function renderDetail(key, animate) {
    const p = personas.find(x => x.key === key);
    if (!p) return;
    const dark = isDark();
    const ac = dark ? p.accentDark : p.accent;
    detailEl.style.setProperty('--pa', ac);
    detailEl.style.setProperty('--pd-gradient', p.gradient);
    detailEl.style.setProperty('--pd-gradient-dark', p.gradientDark);

    detailEl.innerHTML = `
      <div class="meth-pd-art">
        <img src="${p.img}" alt="${p.name}" style="opacity:0;transform:scale(.9)"/>
      </div>
      <div class="meth-pd-copy">
        <div class="meth-pd-badge" style="color:${ac}">${p.badge}</div>
        <div class="meth-pd-name">${p.name}</div>
        <p class="meth-pd-desc">${p.desc}</p>
        <div class="meth-pd-cards">
          <div class="meth-pd-card">
            <div class="meth-pd-card-label">Signature prompt</div>
            <div class="meth-pd-card-text">${p.prompt}</div>
          </div>
          <div class="meth-pd-card">
            <div class="meth-pd-card-label">Flavor note</div>
            <div class="meth-pd-card-text">${p.liner}</div>
          </div>
        </div>
        <div class="meth-pd-axes">
          ${Object.entries(p.axes).map(([k,v])=>`
            <div class="meth-ax">
              <div class="meth-ax-label"><span>${k}</span><span>${v}</span></div>
              <div class="meth-ax-track"><div class="meth-ax-fill" style="width:0%;background:${ac}"></div></div>
            </div>`).join('')}
        </div>
      </div>`;

    requestAnimationFrame(() => {
      const img = detailEl.querySelector('.meth-pd-art img');
      if (img) { img.style.opacity = '1'; img.style.transform = 'scale(1)'; }
      detailEl.querySelectorAll('.meth-ax-fill').forEach((f, i) => {
        const w = Object.values(p.axes)[i];
        setTimeout(() => f.style.width = w + '%', 50 + i * 60);
      });
    });
  }

  function selectPersona(key) {
    activeKey = key;
    renderList();
    renderDetail(key, true);
  }

  listEl.addEventListener('click', e => {
    const btn = e.target.closest('[data-pkey]');
    if (btn) selectPersona(btn.dataset.pkey);
  });

  // Initial render
  renderList();
  renderDetail('architect', false);

  // Re-render on theme change to swap accent colors
  const themeObs = new MutationObserver(() => { renderList(); renderDetail(activeKey, false); });
  themeObs.observe(document.documentElement, { attributes: true, attributeFilter: ['class'] });

  // Intersection Observer for reveals
  const scroll = document.getElementById('methScroll');
  if (!scroll) return;
  const obs = new IntersectionObserver(entries => {
    entries.forEach(e => { if (e.isIntersecting) e.target.classList.add('vis'); });
  }, { threshold: 0.1, root: scroll });
  scroll.querySelectorAll('.meth-reveal').forEach(el => obs.observe(el));

  // Scroll progress + dot nav
  const sections = scroll.querySelectorAll('[data-meth-section]');
  const dots = document.querySelectorAll('.meth-dot');
  const pf = document.getElementById('methProgressFill');

  scroll.addEventListener('scroll', () => {
    const max = scroll.scrollHeight - scroll.clientHeight;
    if (pf) pf.style.width = (max > 0 ? scroll.scrollTop / max * 100 : 0) + '%';
    let cur = 0;
    const sr = scroll.getBoundingClientRect();
    sections.forEach((s, i) => {
      if (s.getBoundingClientRect().top - sr.top < sr.height * 0.5) cur = i;
    });
    dots.forEach((d, i) => d.classList.toggle('active', i === cur));
  });

  dots.forEach(d => d.addEventListener('click', () => {
    const idx = parseInt(d.dataset.methIdx);
    sections[idx]?.scrollIntoView({ behavior:'smooth' });
  }));

  // Reset scroll position when modal opens
  const modal = document.getElementById('methodologyModal');
  if (modal) {
    const mo = new MutationObserver((mutations) => {
      for (const m of mutations) {
        if (m.target.classList.contains('active')) {
          scroll.scrollTop = 0;
          if (pf) pf.style.width = '0%';
          dots.forEach((d, i) => d.classList.toggle('active', i === 0));
          // Re-trigger reveals
          scroll.querySelectorAll('.meth-reveal').forEach(el => el.classList.remove('vis'));
          setTimeout(() => {
            scroll.querySelectorAll('.meth-reveal').forEach(el => obs.observe(el));
          }, 50);
        }
      }
    });
    mo.observe(modal, { attributes: true, attributeFilter: ['class'] });
  }
})();
</script>
